---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

const authors = [
	{
		name: 'Justin Wang',
		title: 'Co-Founder & Principal Researcher',
		linkedin: 'https://www.linkedin.com/in/jstwng/',
		twitter: 'https://x.com/jstwng',
		image: '/justin.jpeg'
	},
	{
		name: 'Rishabh Ranjan',
		title: 'Co-Founder & Principal Researcher',
		linkedin: 'https://www.linkedin.com/in/rishabhranj/',
		twitter: 'https://x.com/rishabh12ranjan',
		image: '/rishabh.jpeg'
	},
	{
		name: 'Ayush Tripathi',
		title: 'Co-Founder & Principal Engineer',
		linkedin: 'https://www.linkedin.com/in/ayush-tripathi-1bb543279/',
		twitter: '#',
		image: '/ayush.jpg'
	}
];
---

<Layout title="About" description="Meet the research team behind Novana">
	<Header currentPath="/about" />
	
	<main class="main">
		<div class="container">
			<article class="article">
				<header class="article-header">
					<div class="novana-definition">
						<h1 class="novana-word">No·va·na</h1>
						<div class="pronunciation-noun-line">
							<span class="pronunciation">/ noʊ'va:nə /</span>
							<span class="word-type">noun</span>
						</div>
						
						<p class="definition">a coined term combining the Latin root <em>nov-</em> ("new") and the Greek root <em>-ana</em> ("again"), symbolizing renewal through innovation or the reinvention of existing ideas using new tools, technologies, or perspectives. Used to describe platforms, systems, or philosophies centered on iterative progress and fresh thinking.</p>
						
						<blockquote class="usage-quote">
							Ex. "<em>Novana embodies a new approach to investment research.</em>"
						</blockquote>
					</div>
					
					<p class="article-blurb">We are in the midst of a major technological revolution — artificial intelligence will fundamentally change the ways in which humans live and our society functions. At Novana, we believe that strides in artificial intelligence will enable previously nascent industries to thrive. Furthermore, we believe that Silicon Valley is the geographic hub that will house it all: the San Francisco Bay Area is where researchers turn founders, scientific innovation finds financial backing, and East meets West.</p>
					
					<p class="article-blurb">Novana marries scientific domain knowledge and end-to-end investment management expertise, critically analyzing emerging technologies so as to produce empirically sound findings for those looking to investment in the future. Our Stanford-based team produces in-house technology analyses, investment research briefs, and market trend writeups across edge computing, biotechnology, artificial intelligence, and other frontier technology industries. </p>
				</header>
			</article>
		</div>

		<section class="team-section">
			<div class="container">
				<div class="team-grid">
					{authors.map(author => (
						<div class="team-member">
							<div class="member-image">
								<img src={author.image} alt={author.name} />
							</div>
							<div class="member-info">
								<h3 class="member-name">{author.name}</h3>
								<p class="member-title">{author.title}</p>
								<div class="member-social">
									<a href={author.linkedin} target="_blank" rel="noopener noreferrer" class="social-link">
										<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
											<path d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"></path>
											<rect x="2" y="9" width="4" height="12"></rect>
											<circle cx="4" cy="4" r="2"></circle>
										</svg>
									</a>
									<a href={author.twitter} target="_blank" rel="noopener noreferrer" class="social-link">
										<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
											<path d="M23 3a10.9 10.9 0 0 1-3.14 1.53 4.48 4.48 0 0 0-7.86 3v1A10.66 10.66 0 0 1 3 4s-4 9 5 13a11.64 11.64 0 0 1-7 2c9 5 20 0 20-11.5a4.5 4.5 0 0 0-.08-.83A7.72 7.72 0 0 0 23 3z"></path>
										</svg>
									</a>
								</div>
							</div>
						</div>
					))}
				</div>
			</div>
		</section>
	</main>
	
	<Footer />
</Layout>

<style>
	.main {
		background: var(--bg-white);
		color: var(--text-primary);
		min-height: calc(100vh - 80px);
	}

	.container {
		max-width: 1160px;
		margin: 0 auto;
		padding: 0 var(--spacing-lg);
	}

	.article {
		padding: var(--spacing-3xl) 0 var(--spacing-sm) 0; /* Further reduced bottom padding from lg to sm */
		text-align: center;
	}

	.article-title {
		font-size: 2.5rem; /* Slightly smaller to match research page proportions */
		font-weight: 700;
		color: var(--text-primary);
		margin-bottom: var(--spacing-xl);
		line-height: 1.1;
	}

	/* Novana Definition Styles */
	.novana-definition {
		margin-bottom: var(--spacing-3xl);
		text-align: left;
		max-width: 900px;
		margin-left: auto;
		margin-right: auto;
	}

	.novana-word {
		font-family: 'Cardo', serif;
		font-size: 3rem;
		font-weight: 700;
		color: var(--text-primary);
		margin-bottom: var(--spacing-xs);
		line-height: 1;
		letter-spacing: 0.05em;
	}

	.pronunciation-noun-line {
		display: flex;
		align-items: center;
		gap: var(--spacing-lg); /* Increased space between pronunciation and noun */
		margin-bottom: var(--spacing-md);
	}

	.pronunciation {
		font-size: 1.1rem;
		color: var(--text-primary);
		font-style: italic;
	}

	.word-type {
		font-size: 1.1rem;
		font-weight: 600;
		color: var(--text-primary);
	}

	.definition {
		font-size: 1.1rem;
		line-height: 1.7;
		color: var(--text-primary);
		margin-bottom: var(--spacing-md);
	}

	.definition em {
		font-style: italic;
		color: var(--text-primary);
	}

	.usage-quote {
		font-size: 1.1rem;
		line-height: 1.7;
		color: var(--text-primary);
		font-style: italic;
		margin: var(--spacing-md) 0 var(--spacing-3xl) 0; /* Added larger bottom margin */
		padding-bottom: var(--spacing-2xl);
		border-bottom: 1px solid var(--text-primary);
	}

	.article-blurb {
		font-size: 1.1rem; /* Slightly smaller to match research page text sizing */
		line-height: 1.6;
		color: var(--text-secondary);
		max-width: 900px;
		margin: 0 auto var(--spacing-lg) auto; /* Add bottom margin for spacing between paragraphs */
		font-weight: 400;
	}

	.article-blurb:last-of-type {
		margin-bottom: 0; /* Remove bottom margin from the last paragraph */
	}

	.team-section {
		padding: 0; /* Reduced from 3xl to xl for closer spacing */
		background: var(--bg-white);
		padding-bottom: var(--spacing-xl); /* Add padding between containers and footer */
	}

	.section-title {
		font-size: 2rem;
		font-weight: 600;
		color: var(--text-primary);
		margin-bottom: var(--spacing-3xl);
		text-align: center;
	}

	.team-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 0; /* Remove gap to connect containers */
		justify-items: stretch; /* Stretch to fill available space */
		align-items: start;
		max-width: 900px; /* Increased from 600px to accommodate 3 members */
		margin: 0 auto; /* Center the grid */
		margin-bottom: var(--spacing-3xl);
	}

	.team-member {
		text-align: center;
		background: var(--bg-white);
		padding: 30px; /* Match article card padding */
		border-top: 1px solid var(--border-color);
		border-bottom: 1px solid var(--border-color);
		border-left: 1px solid var(--border-color);
		min-height: 400px; /* Match article card min-height */
		display: flex;
		flex-direction: column;
	}

	/* Add right border to all team members except remove from middle one to maintain connected look */
	.team-member:last-child {
		border-right: 1px solid var(--border-color);
	}

	.member-image {
		margin-bottom: var(--spacing-md); /* Reduced from xl to md for tighter spacing */
		flex-shrink: 0;
	}

	.member-image img {
		max-width: 240px;
		height: auto;
		object-fit: cover;
		filter: grayscale(100%);
		display: block;
		margin: 0 auto;
	}

	.member-info {
		text-align: left; /* Change to left align */
		flex-grow: 1;
		display: flex;
		flex-direction: column;
	}

	.member-name {
		font-size: 1.4rem; /* Match article title size */
		font-weight: 600;
		color: var(--text-primary);
		margin-bottom: var(--spacing-xs); /* Reduced spacing */
	}

	.member-title {
		font-size: 0.9rem; /* Match article excerpt size */
		color: var(--text-secondary);
		margin-bottom: var(--spacing-md); /* Reduced spacing */
		font-weight: 400;
		flex-grow: 1;
		line-height: 1.4;
	}

	.member-social {
		display: flex;
		gap: var(--spacing-sm); /* Reduced gap */
		justify-content: flex-start; /* Left align social buttons */
		margin-top: auto; /* Push to bottom like article meta */
	}

	.social-link {
		color: var(--text-muted);
		transition: color 0.2s ease;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: var(--spacing-sm);
		border-radius: 0;
		border: 1px solid var(--border-color);
		background: var(--bg-white);
	}

	.social-link:hover {
		color: var(--text-primary);
		border-color: var(--text-muted);
	}

	@media (max-width: 1024px) {
		.team-grid {
			max-width: 100%;
			gap: var(--spacing-sm);
		}

		.team-member {
			padding: var(--spacing-md);
			min-height: 350px;
		}

		.member-image img {
			max-width: 180px;
			height: auto;
		}
	}

	@media (max-width: 768px) {
		.container {
			padding: 0 var(--spacing-md);
		}
		
		.article {
			padding: var(--spacing-2xl) 0 var(--spacing-md) 0;
			text-align: left; /* Left align instead of center */
		}
		
		.article-title {
			font-size: 2rem; /* Maintain proportional scaling */
			text-align: left; /* Left align title */
		}

		/* Mobile styles for Novana definition */
		.novana-word {
			font-size: 2.2rem;
		}

		.pronunciation {
			font-size: 1rem;
		}

		.word-type {
			font-size: 1rem;
		}

		.definition {
			font-size: 1rem;
			line-height: 1.6;
		}

		.usage-quote {
			font-size: 1rem;
			padding-left: var(--spacing-md);
		}

		.section-title {
			font-size: 1.5rem;
		}

		.article-blurb {
			font-size: 1rem; /* Maintain proportional scaling */
			padding: 0 var(--spacing-sm); /* Add side padding */
			text-align: left; /* Left align text */
		}

		.team-section {
			padding: var(--spacing-lg) 0 var(--spacing-xl);
		}

		.team-grid {
			grid-template-columns: 1fr;
			gap: var(--spacing-xl); /* Reduced gap */
			max-width: 100%; /* Use full width */
		}

		.team-member {
			max-width: 100%; /* Use full width */
			padding: var(--spacing-lg);
			min-height: auto; /* Remove fixed height */
		}

		.member-image img {
			max-width: 200px; /* Smaller on mobile */
			height: auto;
		}

		.member-name {
			font-size: 1.2rem; /* Slightly smaller */
		}

		.member-title {
			font-size: 0.9rem;
		}

		.member-social {
			gap: var(--spacing-sm);
		}
	}
	
	/* Small phones */
	@media (max-width: 480px) {
		.container {
			padding: 0 var(--spacing-sm);
		}
		
		.article-title {
			font-size: 1.8rem;
		}
		
		.article-blurb {
			font-size: 0.9rem;
			padding: 0;
		}
		
		.team-member {
			padding: var(--spacing-md);
		}
		
		.member-image img {
			max-width: 160px;
		}
		
		.member-name {
			font-size: 1.1rem;
		}
		
		.member-title {
			font-size: 0.85rem;
		}
	}
</style>